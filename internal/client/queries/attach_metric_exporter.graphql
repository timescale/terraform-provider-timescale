mutation AttachServiceToMetricExporter($projectId: ID!, $serviceId: ID!, $exporterId: ID!){
    attachServiceToMetricExporter(data: {
        projectId: $projectId,
        serviceId: $serviceId,
        exporterId: $exporterId
    })
}
