mutation AttachServiceToPrivateLinkConnection($projectId: ID!, $serviceId: ID!, $connectionIds: [ID!]!) {
    attachServiceToPrivateLinkConnection(data: {
        projectId: $projectId
        serviceId: $serviceId
        connectionIds: $connectionIds
    })
}
