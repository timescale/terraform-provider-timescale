mutation AttachServiceToVPC($projectId: ID!, $serviceId: ID!, $vpcId: ID!) {
    attachServiceToVpc (data:{
        serviceId: $serviceId,
        projectId: $projectId,
        vpcId: $vpcId
    })
}
