mutation DetachServiceFromGenericExporter($projectId: ID!, $serviceId: ID!, $exporterId: String!) {
    detachServiceFromGenericExporter(data: {
        projectId: $projectId,
        serviceId: $serviceId,
        exporterId: $exporterId
    })
}
