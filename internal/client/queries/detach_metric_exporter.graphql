mutation DetachServiceFromMetricExporter($projectId: ID!, $serviceId: ID!, $exporterId: ID!) {
    detachServiceFromMetricExporter(data: {
        projectId: $projectId,
        serviceId: $serviceId,
        exporterId: $exporterId
    })
}
