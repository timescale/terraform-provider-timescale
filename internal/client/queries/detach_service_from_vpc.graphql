mutation DetachServiceFromVPC($projectId: ID!, $serviceId: ID!, $vpcId: ID!) {
    detachServiceFromVpc (data:{
        serviceId: $serviceId,
        projectId: $projectId,
        vpcId: $vpcId
    })
}
