query GetService($projectId: ID!, $serviceId: ID!) {
    getService (data:{
        serviceId: $serviceId,
        projectId: $projectId
    }) {
        id
        projectId
        name
        type
        created
        status
        autoscaleSettings {
            enabled
        }
        regionCode
    }
}
