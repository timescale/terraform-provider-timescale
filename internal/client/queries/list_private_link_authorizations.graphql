query ListPrivateLinkAuthorizations($projectId: ID!) {
    listPrivateLinkAuthorizations(data: {
        projectId: $projectId
    }) {
        projectId
        subscriptionId
        name
        createdAt
        updatedAt
    }
}
