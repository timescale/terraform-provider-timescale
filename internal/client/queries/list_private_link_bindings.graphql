query ListPrivateLinkBindings($projectId: ID!, $serviceId: ID!) {
    listPrivateLinkBindings(data: { projectId: $projectId, serviceId: $serviceId }) {
        projectId
        serviceId
        connectionId
        bindingType
        port
        hostname
        createdAt
    }
}
