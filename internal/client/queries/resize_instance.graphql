mutation ResizeInstance($projectId: ID!, $serviceId: ID!, $config: ResourceConfig!) {
    resizeInstance (data:{
        serviceId: $serviceId,
        projectId: $projectId,
        config: $config
    })
}