mutation SetReplicaCount($projectId: ID!, $serviceId: ID!, $replicaCount: Int!) {
    setReplicaCount (data:{
        serviceId: $serviceId,
        projectId: $projectId,
        replicaCount: $replicaCount
    })
}
