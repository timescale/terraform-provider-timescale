mutation ToggleConnectionPooler($projectId: ID!, $serviceId: ID!, $enable: Boolean!) {
    toggleConnectionPooler (data:{
        serviceId: $serviceId,
        projectId: $projectId,
        enable: $enable
    })
}
